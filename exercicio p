#include<stdio.h>
#include<math.h>
void main ()
{
  int x,sm=0, med, i=0;
  for (x=50;x<=70;x++)
  {
   if ((x%2)==0)
   {
    sm += x;
    i++;
   }
  }
  printf("Somatoria dos numeros pares de 50 a 70 : %d\n", sm);
  med=sm/i;
  printf("Somatoria dos numeros pares de 50 a 70 : %d", med);
}
